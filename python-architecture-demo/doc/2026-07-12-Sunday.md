# Sunday, 7/12/2026 Worklog

- Added additional routes
- Moved logic into more feature-based structure
- Need to decide where any _domain_ should reside

## `entities.py`

- Could model business logic entities in each feature slice in an `entity.py` or `entities.py` file
- If a lot of entities, could model them in separate code files under an `entities` _folder_

## `exceptions.py`

- Custom business-case exceptions for this feature, slice
- Optional

## `models.py`

- The location for objects mapped to the database, otherewise known as `DTO`'s or _Data Transfer Objects_
- Imports `sqlalchemy` and related classes
- Imports database-related base class
- May rename the database-related import `app.database`
- All features could import `app.database` or whatever

## `routes.py`

`routes` → `services` → `repositories` → `database`

- Imports `FASTApi`-related classes
- Currently imports `sqlalchemy.ext.asyncio` in the `health` feature, not sure this is appropriate

## `schemas.py`

- The location for request/response classes used by the api's/routes

## `repositories.py`

- Wrapper around any database access
- Can be used by `services.py`
- Optional

## `services.py`

- Direct SQL against the database

## Infrastructure

- Anything specific to this feature-slice in terms of cross-cutting concerns
- Location for repositories.
- Could insure that mapped database object definitions are localized to the and infrastructure folders/layers

## Notes

```text
- Review the code structure in the `python-architecture-demo` app
- Comment on best-practices for code location/modularity
- Where should domain-specific class/code logic reside in this structure?
```